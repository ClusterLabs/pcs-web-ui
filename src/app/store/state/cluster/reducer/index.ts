import clusterStorage from "./storage";
import { clusterStatusDefault, compareStatusSeverity } from "./clusterStatus";

export { clusterStatusDefault, compareStatusSeverity };

export default clusterStorage;

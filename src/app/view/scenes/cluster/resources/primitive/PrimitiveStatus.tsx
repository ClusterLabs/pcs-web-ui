import React from "react";
import { types } from "app/store";

export const PrimitiveStatus = ({
  primitive,
}: {
  primitive: types.cluster.Primitive;
}) => {
  return <>Primitive status here</>;
};

import React from "react";

import { Primitive } from "app/services/cluster/types";

const PrimitiveConstraints = ({ primitive }: {
  primitive: Primitive,
}) => {
  return (
    <>
      Constraints
    </>
  );
};

export default PrimitiveConstraints;

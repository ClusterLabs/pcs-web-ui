import {endpoint} from "./endpoint";

export const resourceClone = endpoint({
  url: "/managec/resource_clone",
  method: "post",
  params: undefined,
  payload: undefined,
  validate: undefined,
  shape: undefined,
});

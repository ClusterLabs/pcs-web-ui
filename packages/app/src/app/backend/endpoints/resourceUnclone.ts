import {endpoint} from "./endpoint";

export const resourceUnclone = endpoint({
  url: "/managec/resource_unclone",
  method: "post",
  params: undefined,
  payload: undefined,
  validate: undefined,
  shape: undefined,
});
